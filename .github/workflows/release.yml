name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force:
        description: Rebuild and publish even when the GitHub Release already exists.
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  packages: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  pack:
    runs-on: ubuntu-latest
    env:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5.4.0
        with:
          dotnet-version: 10.0.x

      - name: Resolve release version
        id: release
        env:
          FORCE_RELEASE: ${{ github.event_name == 'workflow_dispatch' && inputs.force }}
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/release.sh resolve

      - name: Validate publishing credentials
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: test -n "$NUGET_API_KEY" || { echo "::error::NUGET_API_KEY is required to publish this release."; exit 1; }

      - name: Restore tools
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: dotnet tool restore

      - name: Restore
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: dotnet restore ManagedCode.LlmTck.slnx

      - name: Format
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: dotnet format ManagedCode.LlmTck.slnx --verify-no-changes --no-restore

      - name: Build
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: dotnet build ManagedCode.LlmTck.slnx --configuration Release --no-restore

      - name: Test
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: bash scripts/coverage.sh

      - name: Pack
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: for project in src/*/*.csproj; do dotnet pack "$project" --configuration Release --no-build --output artifacts/packages; done

      - name: Verify installed NuGet consumer
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: python3 scripts/verify-package.py

      - name: Verify release recovery
        if: ${{ steps.release.outputs.should_release == 'true' }}
        run: python3 scripts/test-release.py

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: verification-evidence
          path: |
            artifacts/coverage
            artifacts/package-consumer

      - name: Publish container image
        if: ${{ steps.release.outputs.should_release == 'true' }}
        env:
          IMAGE_TAG: ${{ steps.release.outputs.version }}
        run: |
          echo "${{ github.token }}" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
          docker build \
            -f samples/ManagedCode.LlmTck.Service/Dockerfile \
            -t "ghcr.io/managedcode/llm-tck:$IMAGE_TAG" \
            .
          docker push "ghcr.io/managedcode/llm-tck:$IMAGE_TAG"

      - name: Publish packages and finalize release
        if: ${{ steps.release.outputs.should_release == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ steps.release.outputs.tag }}
        run: bash scripts/release.sh publish
