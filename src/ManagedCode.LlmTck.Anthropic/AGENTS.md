# ManagedCode.LlmTck.Anthropic

This package owns Anthropic compatibility constants, provider profiles, and future Messages API wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
