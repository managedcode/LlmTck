# ManagedCode.LlmTck.Perplexity

This package owns Perplexity compatibility constants, provider profiles, and future Perplexity wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
