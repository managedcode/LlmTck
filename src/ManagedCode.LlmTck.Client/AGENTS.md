# ManagedCode.LlmTck.Client

This package contains the control client and `Microsoft.Extensions.AI` implementations.

Use the official `Microsoft.Extensions.AI` interfaces directly. Avoid provider SDK dependencies unless a provider-specific client package is intentionally added.
