# ManagedCode.LlmTck.AzureOpenAI

This package owns Azure OpenAI compatibility constants, provider profiles, and future Azure OpenAI wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
