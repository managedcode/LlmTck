# ManagedCode.LlmTck.OpenAI

This package owns OpenAI-compatible wire contracts and mapping only.

Keep DTOs explicit with `JsonPropertyName`. Do not add runtime state or ASP.NET Core dependencies here.
