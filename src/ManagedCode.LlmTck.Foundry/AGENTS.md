# ManagedCode.LlmTck.Foundry

This package owns Microsoft Foundry compatibility constants, provider profiles, and future Foundry wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
