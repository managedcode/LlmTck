@namespace ManagedCode.LlmTck.Hosting
@page "/"
@using Microsoft.AspNetCore.Components.Web

<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<meta name="generator" content="Blazor SSR">
<link rel="icon" href="data:,">
<title>LLM TCK · Request dashboard</title>
<style>
  :root {
    color-scheme: dark;
    --canvas: #070a0f;
    --pane: #0b1018;
    --pane-raised: #101722;
    --pane-selected: #14231f;
    --hover: #111b28;
    --line: #202c3c;
    --line-soft: rgb(32 44 60 / 62%);
    --text: #f3f6fa;
    --text-soft: #c3cdda;
    --muted: #8090a3;
    --accent: #68dfb8;
    --accent-strong: #2fc798;
    --blue: #79aaff;
    --green: #61d89c;
    --red: #ff7e88;
    --amber: #edb45e;
    --violet: #b39cff;
  }

  * { box-sizing: border-box; }
  html, body { width: 100%; height: 100%; }
  html { background: var(--canvas); }
  body {
    margin: 0;
    min-width: 320px;
    overflow: hidden;
    background: var(--canvas);
    color: var(--text);
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    line-height: 1.42;
    -webkit-font-smoothing: antialiased;
  }

  button, input, select, summary { font: inherit; }
  button, input, select, summary { outline: none; }
  button:focus-visible, input:focus-visible, select:focus-visible, summary:focus-visible {
    box-shadow: 0 0 0 2px var(--canvas), 0 0 0 4px rgb(104 223 184 / 62%);
  }
  code, pre, .mono { font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace; }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .shell {
    position: relative;
    display: grid;
    grid-template-rows: 58px minmax(0, 1fr);
    width: 100%;
    height: 100vh;
    height: 100svh;
    overflow: hidden;
    background:
      radial-gradient(circle at 8% -20%, rgb(47 199 152 / 8%), transparent 30rem),
      var(--canvas);
  }

  .topbar {
    position: relative;
    z-index: 20;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    min-width: 0;
    padding: 0 14px;
    border-bottom: 1px solid var(--line);
    background: rgb(7 10 15 / 94%);
    backdrop-filter: blur(16px);
  }
  .brand { display: flex; align-items: center; gap: 10px; min-width: 170px; }
  .brand-mark {
    display: grid;
    place-items: center;
    width: 32px;
    height: 32px;
    border: 1px solid rgb(104 223 184 / 45%);
    border-radius: 8px;
    background: rgb(104 223 184 / 10%);
    color: var(--accent);
    font-weight: 900;
  }
  .brand-copy { min-width: 0; }
  .brand h1 { margin: 1px 0 0; font-size: 15px; line-height: 1.1; letter-spacing: -.02em; }
  .eyebrow {
    color: var(--muted);
    font-size: 9px;
    font-weight: 850;
    letter-spacing: .13em;
    text-transform: uppercase;
  }

  .command-bar {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 7px;
    min-width: 0;
  }
  .header-token input,
  .search-field input,
  .compact-filters select,
  .rail-select select {
    width: 100%;
    border: 1px solid var(--line);
    border-radius: 7px;
    background: #080c12;
    color: var(--text);
  }
  .header-token { width: clamp(140px, 17vw, 230px); }
  .header-token input { height: 34px; padding: 0 10px; font-size: 12px; }
  input::placeholder { color: #5e6d80; }
  .toolbar-button,
  .auto-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 34px;
    padding: 0 10px;
    border: 1px solid var(--line);
    border-radius: 7px;
    background: var(--pane-raised);
    color: var(--text-soft);
    cursor: pointer;
    font-size: 11px;
    font-weight: 800;
    white-space: nowrap;
  }
  .toolbar-button:hover, .auto-toggle:hover { border-color: #3c4b61; background: var(--hover); }
  .toolbar-button.accent { border-color: rgb(104 223 184 / 58%); color: var(--accent); }
  .toolbar-button.danger:hover { border-color: rgb(255 126 136 / 62%); color: var(--red); }
  .toolbar-button:disabled { opacity: .45; cursor: not-allowed; }
  .auto-toggle { gap: 6px; user-select: none; }
  .auto-toggle input { accent-color: var(--accent-strong); }

  .connection {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    min-height: 34px;
    padding: 0 9px;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: var(--text-soft);
    font-size: 11px;
    font-weight: 800;
    white-space: nowrap;
  }
  .status-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--muted); }
  .connection.ok .status-dot { background: var(--green); box-shadow: 0 0 10px rgb(97 216 156 / 70%); }
  .connection.warn .status-dot { background: var(--amber); }
  .connection.err .status-dot { background: var(--red); }

  .notice {
    display: none;
    position: fixed;
    z-index: 50;
    top: 66px;
    left: 50%;
    width: min(560px, calc(100vw - 28px));
    padding: 10px 12px;
    transform: translateX(-50%);
    border: 1px solid var(--line);
    border-radius: 8px;
    background: #111923;
    color: var(--text-soft);
    box-shadow: 0 18px 48px rgb(0 0 0 / 42%);
    font-size: 12px;
  }
  .notice.show { display: block; }
  .notice.err { border-color: rgb(255 126 136 / 60%); color: var(--red); }
  .notice.warn { border-color: rgb(237 180 94 / 60%); color: var(--amber); }

  .mail-workspace {
    display: grid;
    grid-template-columns: 188px minmax(360px, 410px) minmax(520px, 1fr);
    min-width: 0;
    min-height: 0;
    overflow: hidden;
  }

  .nav-rail,
  .request-inbox,
  .reading-pane {
    min-width: 0;
    min-height: 0;
    background: var(--pane);
  }
  .nav-rail {
    display: flex;
    flex-direction: column;
    border-right: 1px solid var(--line);
    background: #090e15;
  }
  .rail-scroll { min-height: 0; overflow: auto; padding: 12px 9px; }
  .rail-section { padding: 4px 0 13px; border-bottom: 1px solid var(--line-soft); }
  .rail-section + .rail-section { padding-top: 13px; }
  .rail-section:last-child { border-bottom: 0; }
  .rail-heading {
    margin: 0 8px 7px;
    color: var(--muted);
    font-size: 9px;
    font-weight: 850;
    letter-spacing: .1em;
    text-transform: uppercase;
  }
  .rail-section-title { display: flex; align-items: center; justify-content: space-between; }
  .rail-clear {
    margin: 0 7px 6px;
    padding: 0;
    border: 0;
    background: transparent;
    color: var(--accent);
    cursor: pointer;
    font-size: 10px;
  }
  .rail-item {
    display: grid;
    grid-template-columns: 22px minmax(0, 1fr) auto;
    align-items: center;
    gap: 7px;
    width: 100%;
    min-height: 34px;
    padding: 5px 8px;
    border: 0;
    border-radius: 6px;
    background: transparent;
    color: var(--text-soft);
    cursor: pointer;
    text-align: left;
    font-size: 11px;
  }
  .rail-item:hover { background: var(--hover); }
  .rail-item.selected { background: rgb(104 223 184 / 10%); color: var(--text); }
  .rail-item strong { color: var(--muted); font-size: 10px; font-variant-numeric: tabular-nums; }
  .rail-icon {
    display: grid;
    place-items: center;
    width: 19px;
    height: 19px;
    border-radius: 5px;
    background: rgb(128 144 163 / 10%);
    color: var(--muted);
    font-size: 10px;
    font-weight: 900;
  }
  .rail-icon.success { color: var(--green); }
  .rail-icon.error { color: var(--red); }
  .rail-icon.stream { color: var(--violet); }
  .rail-icon.cache { color: var(--accent); }
  .rail-item.provider { grid-template-columns: 10px minmax(0, 1fr) auto; }
  .provider-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--blue); }
  .rail-empty { padding: 8px; color: var(--muted); font-size: 10px; }
  .rail-select { display: grid; gap: 6px; }
  .rail-select select { height: 34px; padding: 0 8px; font-size: 11px; }
  .clear-filters {
    width: 100%;
    margin-top: 8px;
    padding: 7px;
    border: 1px solid var(--line);
    border-radius: 6px;
    background: transparent;
    color: var(--muted);
    cursor: pointer;
    font-size: 10px;
  }
  .rail-footer {
    display: grid;
    gap: 2px;
    margin-top: auto;
    padding: 10px 12px;
    border-top: 1px solid var(--line);
    color: var(--muted);
    font-size: 9px;
  }

  .request-inbox {
    display: grid;
    grid-template-rows: auto auto auto minmax(0, 1fr);
    border-right: 1px solid var(--line);
  }
  .inbox-header,
  .reading-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 13px 14px;
    border-bottom: 1px solid var(--line);
    background: var(--pane-raised);
  }
  .inbox-header h2, .reading-header h2, .diagnostics-header h2 {
    margin: 0;
    font-size: 14px;
    letter-spacing: -.015em;
  }
  .inbox-header p { margin: 2px 0 0; color: var(--muted); font-size: 10px; }
  .count-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 24px;
    min-height: 22px;
    padding: 0 7px;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: var(--text-soft);
    font-size: 10px;
    font-weight: 850;
  }
  .inbox-tools { display: grid; gap: 7px; padding: 9px 10px; border-bottom: 1px solid var(--line-soft); }
  .search-field input { height: 34px; padding: 0 10px; font-size: 11px; }
  .compact-filters { display: none; grid-template-columns: repeat(3, minmax(0, 1fr)) auto; gap: 6px; }
  .compact-filters select { min-width: 0; height: 32px; padding: 0 7px; font-size: 10px; }
  .compact-filters .toolbar-button { min-height: 32px; }
  .inbox-columns {
    display: grid;
    grid-template-columns: 1fr auto;
    padding: 7px 12px;
    border-bottom: 1px solid var(--line);
    color: var(--muted);
    font-size: 8px;
    font-weight: 850;
    letter-spacing: .1em;
    text-transform: uppercase;
  }
  .inbox-list { min-height: 0; overflow: auto; }
  .inbox-item {
    position: relative;
    display: grid;
    gap: 5px;
    width: 100%;
    min-height: 92px;
    padding: 10px 12px 10px 14px;
    border: 0;
    border-bottom: 1px solid var(--line-soft);
    background: transparent;
    color: var(--text-soft);
    cursor: pointer;
    text-align: left;
  }
  .inbox-item::before {
    content: "";
    position: absolute;
    inset: 0 auto 0 0;
    width: 3px;
    background: transparent;
  }
  .inbox-item:hover { background: var(--hover); }
  .inbox-item.selected { background: var(--pane-selected); }
  .inbox-item.selected::before { background: var(--accent); }
  .inbox-item-top {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 7px;
  }
  .inbox-item-top time { color: var(--muted); font-size: 9px; font-variant-numeric: tabular-nums; }
  .inbox-model { overflow: hidden; color: var(--text); font-size: 11px; font-weight: 750; text-overflow: ellipsis; white-space: nowrap; }
  .inbox-endpoint { display: grid; grid-template-columns: auto minmax(0, 1fr); align-items: center; gap: 6px; }
  .inbox-endpoint .mono { overflow: hidden; color: var(--text-soft); font-size: 10px; text-overflow: ellipsis; white-space: nowrap; }
  .inbox-operation { overflow: hidden; color: var(--muted); font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }
  .inbox-item-bottom { display: flex; align-items: center; gap: 7px; min-width: 0; overflow: hidden; }
  .inbox-stat { color: var(--muted); font-size: 9px; white-space: nowrap; }
  .inbox-stat.cache { color: var(--accent); }

  .provider-chip, .status-chip, .method-chip, .mode-chip {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    max-width: 100%;
    border: 1px solid var(--chip-tone, var(--line));
    background: color-mix(in srgb, var(--chip-tone, var(--muted)) 10%, transparent);
    color: var(--chip-tone, var(--text-soft));
    white-space: nowrap;
  }
  .provider-chip { --chip-tone: var(--blue); padding: 2px 6px; border-radius: 5px; font-size: 9px; font-weight: 850; }
  .method-chip { --chip-tone: var(--violet); padding: 1px 4px; border-radius: 4px; font-size: 8px; font-weight: 900; }
  .status-chip { padding: 2px 6px; border-radius: 999px; font-size: 9px; font-weight: 850; }
  .status-chip.success { --chip-tone: var(--green); }
  .status-chip.error { --chip-tone: var(--red); }
  .status-chip.warning { --chip-tone: var(--amber); }
  .mode-chip { --chip-tone: var(--violet); padding: 1px 5px; border-radius: 999px; font-size: 8px; font-weight: 850; }

  .reading-pane {
    display: flex;
    flex-direction: column;
    overflow: hidden;
    background: #0a0f17;
  }
  .reading-header { flex: 0 0 auto; align-items: flex-start; padding: 12px 15px; }
  .reading-title { min-width: 0; flex: 1 1 auto; }
  .reading-eyebrow { display: flex; align-items: center; gap: 6px; margin-bottom: 6px; flex-wrap: wrap; }
  .reading-title h2 { font-size: 17px; }
  .reading-route { margin-top: 3px; overflow: hidden; color: var(--muted); font-size: 10px; text-overflow: ellipsis; white-space: nowrap; }
  .reading-id { max-width: 210px; overflow: hidden; color: var(--muted); font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }
  .mobile-back { display: none; }
  .reading-empty, .empty-state {
    display: grid;
    place-items: center;
    min-height: 0;
    padding: 32px;
    color: var(--muted);
    text-align: center;
  }
  .reading-empty { height: 100%; }
  .reading-empty strong, .empty-state strong { display: block; margin-top: 8px; color: var(--text-soft); }
  .reading-empty span, .empty-state span { display: block; margin-top: 4px; font-size: 11px; }
  .reading-empty-icon {
    display: grid;
    place-items: center;
    width: 38px;
    height: 38px;
    margin: auto;
    border: 1px solid var(--line);
    border-radius: 9px;
    color: var(--accent);
  }

  .reading-summary {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    flex: 0 0 auto;
    border-bottom: 1px solid var(--line-soft);
    background: #090e15;
  }
  .summary-item { min-width: 0; padding: 8px 10px; border-right: 1px solid var(--line-soft); border-bottom: 1px solid var(--line-soft); }
  .summary-item:nth-child(3n) { border-right: 0; }
  .summary-item:nth-last-child(-n + 3) { border-bottom: 0; }
  .summary-item span, .meta-label, .usage-label {
    display: block;
    color: var(--muted);
    font-size: 8px;
    font-weight: 850;
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .summary-item strong { display: block; margin-top: 3px; overflow: hidden; color: var(--text-soft); font-size: 10px; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }

  .usage-strip {
    display: grid;
    grid-template-columns: repeat(6, minmax(0, 1fr));
    flex: 0 0 auto;
    border-bottom: 1px solid var(--line);
  }
  .usage-cell { min-width: 0; padding: 7px 9px; border-right: 1px solid var(--line-soft); }
  .usage-cell:last-child { border-right: 0; }
  .usage-value { margin-top: 2px; color: var(--text); font-size: 13px; font-weight: 850; font-variant-numeric: tabular-nums; }
  .usage-cell.cache .usage-value { color: var(--accent); }
  .usage-cell.reasoning .usage-value { color: var(--amber); }

  .detail-tabs {
    display: flex;
    flex: 0 0 auto;
    gap: 2px;
    padding: 6px 10px 0;
    border-bottom: 1px solid var(--line);
    background: var(--pane);
    overflow-x: auto;
  }
  .detail-tab {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    min-height: 34px;
    padding: 0 10px;
    border: 0;
    border-bottom: 2px solid transparent;
    background: transparent;
    color: var(--muted);
    cursor: pointer;
    font-size: 10px;
    font-weight: 800;
    white-space: nowrap;
  }
  .detail-tab span { min-width: 18px; padding: 1px 5px; border-radius: 999px; background: var(--pane-raised); font-size: 8px; }
  .detail-tab:hover { color: var(--text-soft); }
  .detail-tab.selected { border-bottom-color: var(--accent); color: var(--text); }

  .detail-panels { flex: 1 1 auto; min-height: 0; overflow: hidden; }
  .detail-panel { display: none; height: 100%; min-height: 0; overflow: hidden; }
  .detail-panel.active { display: flex; flex-direction: column; }
  .panel-heading {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    min-height: 44px;
    padding: 8px 13px;
    border-bottom: 1px solid var(--line-soft);
  }
  .panel-heading h3 { margin: 0; font-size: 11px; }
  .panel-heading span { color: var(--muted); font-size: 9px; }
  .retention-warning { flex: 0 0 auto; padding: 7px 12px; border-bottom: 1px solid rgb(237 180 94 / 30%); background: rgb(237 180 94 / 8%); color: var(--amber); font-size: 10px; }

  .conversation-scroll { flex: 1 1 auto; min-height: 0; overflow: auto; padding: 12px; }
  .conversation { display: grid; gap: 8px; max-width: 920px; margin: 0 auto; }
  .turn {
    position: relative;
    padding: 9px 10px 10px 13px;
    border: 1px solid var(--line-soft);
    border-radius: 7px;
    background: var(--pane-raised);
    overflow: hidden;
  }
  .turn::before { content: ""; position: absolute; inset: 0 auto 0 0; width: 3px; background: var(--role-tone, var(--muted)); }
  .turn.system { --role-tone: var(--violet); }
  .turn.user { --role-tone: var(--blue); }
  .turn.assistant { --role-tone: var(--accent); }
  .turn.tool { --role-tone: var(--amber); }
  .turn.error { --role-tone: var(--red); }
  .turn-role { color: var(--role-tone, var(--muted)); font-size: 8px; font-weight: 900; letter-spacing: .09em; text-transform: uppercase; }
  .turn-content { margin-top: 5px; color: var(--text-soft); font-size: 11px; white-space: pre-wrap; overflow-wrap: anywhere; }

  .wire-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    flex: 1 1 auto;
    min-height: 0;
    background: var(--line);
  }
  .payload-card { display: flex; min-width: 0; min-height: 0; flex-direction: column; background: #080c12; overflow: hidden; }
  .payload-head {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    padding: 8px 10px;
    border-bottom: 1px solid var(--line-soft);
    color: var(--muted);
    font-size: 8px;
    font-weight: 850;
    letter-spacing: .07em;
    text-transform: uppercase;
  }
  .payload-content { flex: 1 1 auto; min-height: 0; margin: 0; padding: 11px; overflow: auto; color: #d7e0eb; font-size: 10px; line-height: 1.55; white-space: pre-wrap; overflow-wrap: anywhere; }

  .chunk-list { flex: 1 1 auto; min-height: 0; margin: 0; padding: 10px; overflow: auto; list-style: none; }
  .chunk { display: grid; grid-template-columns: 32px minmax(0, 1fr); gap: 8px; padding: 8px 9px; border-bottom: 1px solid var(--line-soft); }
  .chunk-index { color: var(--muted); font-size: 9px; font-weight: 800; }
  .chunk-content { color: var(--text-soft); font-size: 10px; white-space: pre-wrap; overflow-wrap: anywhere; }

  .runtime-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; padding: 12px; overflow: auto; }
  .meta-card { min-width: 0; padding: 9px 10px; border: 1px solid var(--line-soft); border-radius: 7px; background: var(--pane-raised); }
  .meta-value { margin-top: 4px; color: var(--text-soft); font-size: 10px; overflow-wrap: anywhere; }
  .runtime-note { margin: 0 12px 12px; padding: 10px; border: 1px solid var(--line-soft); border-radius: 7px; background: var(--pane-raised); }
  .runtime-note h4 { margin: 0; font-size: 10px; }
  .runtime-note p { margin: 5px 0 0; color: var(--text-soft); font-size: 10px; }

  .diagnostics-sheet {
    position: absolute;
    z-index: 100;
    top: 42px;
    right: -74px;
    width: min(920px, calc(100vw - 24px));
    max-height: calc(100vh - 76px);
    overflow: auto;
    border: 1px solid var(--line);
    border-radius: 10px;
    background: #0b111a;
    box-shadow: 0 24px 80px rgb(0 0 0 / 60%);
  }
  .diagnostics-header { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 13px 14px; border-bottom: 1px solid var(--line); }
  .retention-note { color: var(--muted); font-size: 9px; }
  .diagnostics-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 8px; padding: 10px; }
  .diagnostic-card { min-width: 0; border: 1px solid var(--line-soft); border-radius: 8px; background: var(--pane); overflow: hidden; }
  .diagnostic-card h3 { display: flex; align-items: center; justify-content: space-between; gap: 8px; margin: 0; padding: 8px 9px; border-bottom: 1px solid var(--line-soft); font-size: 10px; }
  .diagnostic-card:last-child { grid-column: span 2; }
  .diagnostic-body { max-height: 240px; overflow: auto; }
  .diagnostic-list { margin: 0; padding: 0; list-style: none; }
  .diagnostic-row { display: grid; gap: 2px; padding: 7px 9px; border-top: 1px solid var(--line-soft); }
  .diagnostic-row:first-child { border-top: 0; }
  .diagnostic-primary { color: var(--text-soft); font-size: 9px; overflow-wrap: anywhere; }
  .diagnostic-secondary { color: var(--muted); font-size: 8px; overflow-wrap: anywhere; }
  .diagnostic-empty { padding: 22px 10px; color: var(--muted); font-size: 10px; text-align: center; }
  .event-line { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 6px; }

  @@media (min-width: 1600px) {
    .mail-workspace { grid-template-columns: 200px 440px minmax(620px, 1fr); }
  }

  @@media (max-width: 1199px) {
    .mail-workspace { grid-template-columns: clamp(330px, 38vw, 390px) minmax(0, 1fr); }
    .nav-rail { display: none; }
    .compact-filters { display: grid; }
    .header-token { width: 150px; }
    .reading-id { max-width: 130px; }
  }

  @@media (max-width: 900px) {
    .mail-workspace { grid-template-columns: 330px minmax(0, 1fr); }
    .reading-summary { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .summary-item:nth-child(n) { border-right: 1px solid var(--line-soft); border-bottom: 1px solid var(--line-soft); }
    .summary-item:nth-child(2n) { border-right: 0; }
    .summary-item:nth-last-child(-n + 2) { border-bottom: 0; }
    .usage-strip { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .usage-cell:nth-child(3n) { border-right: 0; }
    .usage-cell:nth-child(-n + 3) { border-bottom: 1px solid var(--line-soft); }
    .header-token { display: none; }
    .diagnostics-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .diagnostic-card:last-child { grid-column: span 2; }
  }

  @@media (max-width: 767px) {
    .shell { grid-template-rows: 54px minmax(0, 1fr); }
    .topbar { padding: 0 9px; }
    .brand { min-width: 0; }
    .brand-mark { display: none; }
    .brand .eyebrow { display: none; }
    .brand h1 { font-size: 14px; }
    .auto-toggle span { display: none; }
    .toolbar-button { padding: 0 8px; }
    .connection { min-width: 30px; padding: 0 8px; }
    .connection span:last-child { display: none; }
    .mail-workspace { grid-template-columns: minmax(0, 1fr); }
    .request-inbox, .reading-pane { grid-column: 1; grid-row: 1; }
    .mobile-list .reading-pane { display: none; }
    .mobile-reading .request-inbox { display: none; }
    .mobile-back {
      display: inline-flex;
      align-items: center;
      min-height: 30px;
      padding: 0 8px;
      border: 1px solid var(--line);
      border-radius: 6px;
      background: transparent;
      color: var(--text-soft);
      cursor: pointer;
      font-size: 10px;
    }
    .reading-header { align-items: flex-start; }
    .reading-id { display: none; }
    .reading-summary { overflow-x: auto; }
    .wire-grid {
      grid-template-columns: minmax(88vw, 1fr) minmax(88vw, 1fr);
      overflow-x: auto;
      scroll-snap-type: x mandatory;
    }
    .payload-card { scroll-snap-align: start; }
    .diagnostics-sheet { position: fixed; top: 60px; right: 8px; left: 8px; width: auto; max-height: calc(100vh - 68px); }
    .diagnostics-grid { grid-template-columns: 1fr; }
    .diagnostic-card:last-child { grid-column: auto; }
  }

  @@media (max-width: 480px) {
    .compact-filters { grid-template-columns: 1fr 1fr; }
    .compact-filters select:first-child { grid-column: 1 / -1; }
    .inbox-item { min-height: 88px; }
    .reading-title h2 { font-size: 15px; }
    .reading-summary { grid-template-columns: repeat(2, minmax(130px, 1fr)); }
    .usage-strip { overflow-x: auto; grid-template-columns: repeat(6, minmax(74px, 1fr)); }
  }

  @@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { scroll-behavior: auto !important; transition-duration: .01ms !important; }
  }
</style>
<style>
  /* Product skin: Apple-inspired split-view hierarchy and material depth. */
  :root {
    color-scheme: light;
    --canvas: #f5f5f7;
    --pane: #ffffff;
    --pane-raised: #ffffff;
    --pane-selected: #e8f2ff;
    --hover: rgb(118 118 128 / 8%);
    --line: rgb(60 60 67 / 18%);
    --line-soft: rgb(60 60 67 / 10%);
    --text: #1d1d1f;
    --text-soft: #3a3a3c;
    --muted: #6e6e73;
    --accent: #007aff;
    --accent-strong: #0066d6;
    --blue: #007aff;
    --green: #248a3d;
    --red: #d70015;
    --amber: #a05a00;
    --violet: #8944ab;
  }

  html, body { background: var(--canvas); }
  body {
    color: var(--text);
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Helvetica Neue", sans-serif;
    font-size: 14px;
    line-height: 1.45;
  }
  button:focus-visible, input:focus-visible, select:focus-visible, summary:focus-visible {
    box-shadow: 0 0 0 2px #fff, 0 0 0 5px rgb(0 122 255 / 38%);
  }
  .shell {
    grid-template-rows: 52px minmax(0, 1fr);
    background:
      radial-gradient(circle at 12% -20%, rgb(0 122 255 / 10%), transparent 33rem),
      radial-gradient(circle at 95% -25%, rgb(175 82 222 / 7%), transparent 30rem),
      var(--canvas);
  }

  .topbar {
    gap: 18px;
    padding: 0 14px;
    border-bottom: 1px solid rgb(60 60 67 / 16%);
    background: rgb(250 250 252 / 76%);
    color: #1d1d1f;
    backdrop-filter: saturate(180%) blur(26px);
  }
  .brand { min-width: 248px; gap: 10px; }
  .brand-mark {
    width: 32px;
    height: 32px;
    border: 0;
    border-radius: 8px;
    background: linear-gradient(145deg, #5ac8fa, #007aff 52%, #5856d6);
    color: #fff;
    box-shadow: 0 2px 6px rgb(0 85 180 / 24%), inset 0 1px rgb(255 255 255 / 42%);
    font-size: 15px;
  }
  .brand h1 { color: #1d1d1f; font-size: 15px; font-weight: 650; letter-spacing: -.015em; }
  .brand .eyebrow { display: none; }
  .brand-short { display: none; }
  .command-bar { gap: 6px; }
  .toolbar-button,
  .auto-toggle,
  .connection {
    min-height: 32px;
    border: 0;
    border-radius: 9px;
    font-size: 12px;
    font-weight: 600;
  }
  .toolbar-button { gap: 5px; }
  .toolbar-button,
  .auto-toggle {
    background: rgb(118 118 128 / 10%);
    color: #1d1d1f;
  }
  .toolbar-button:hover,
  .auto-toggle:hover { border-color: transparent; background: rgb(118 118 128 / 18%); }
  .toolbar-button.accent { border: 0; background: rgb(0 122 255 / 10%); color: #007aff; box-shadow: none; }
  .toolbar-button.danger { border: 0; background: rgb(255 59 48 / 10%); color: #d70015; }
  .toolbar-button.danger:hover { border-color: transparent; background: rgb(255 59 48 / 16%); color: #d70015; }
  .connection { padding: 0 8px; background: transparent; color: #3a3a3c; }
  .status-dot { width: 7px; height: 7px; background: #8e8e93; }
  .connection.ok .status-dot { background: #30d158; box-shadow: none; }
  .connection.warn .status-dot { background: #ff9f0a; }
  .connection.err .status-dot { background: #ff453a; }
  .auto-toggle input { accent-color: #007aff; }

  .notice {
    top: 60px;
    border: 1px solid #d2d7dc;
    background: #fff;
    color: var(--text-soft);
    box-shadow: 0 8px 24px rgb(0 0 0 / 12%);
  }
  .notice.err { border-color: rgb(255 59 48 / 32%); background: #fff4f3; color: #d70015; }
  .notice.warn { border-color: rgb(255 159 10 / 34%); background: #fff8e8; color: #a05a00; }

  .mail-workspace { grid-template-columns: 216px minmax(360px, 400px) minmax(560px, 1fr); }
  .nav-rail,
  .request-inbox,
  .reading-pane { background: #fff; }
  .nav-rail {
    border-right: 1px solid var(--line);
    background: rgb(242 242 247 / 76%);
    backdrop-filter: saturate(160%) blur(24px);
  }
  .rail-scroll { padding: 14px 10px; }
  .rail-section { padding: 4px 0 14px; border-bottom: 0; }
  .rail-section + .rail-section { padding-top: 9px; }
  .rail-heading {
    margin: 0 9px 7px;
    color: #6e6e73;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: .02em;
    text-transform: none;
  }
  .rail-clear { color: #007aff; font-size: 11px; }
  .rail-item {
    grid-template-columns: 26px minmax(0, 1fr) auto;
    gap: 8px;
    min-height: 36px;
    padding: 5px 8px;
    border-radius: 6px;
    color: #3a3a3c;
    font-size: 12px;
  }
  .rail-item:hover { background: rgb(118 118 128 / 8%); }
  .rail-item.selected { background: rgb(0 122 255 / 13%); color: #005ecb; }
  .rail-item strong { color: #8e8e93; font-size: 11px; font-weight: 500; }
  .rail-icon {
    width: 22px;
    height: 22px;
    border-radius: 5px;
    background: rgb(118 118 128 / 10%);
    color: #636366;
    font-size: 11px;
    font-weight: 600;
  }
  .rail-icon.success { color: #248a3d; background: rgb(52 199 89 / 12%); }
  .rail-icon.error { color: #d70015; background: rgb(255 59 48 / 10%); }
  .rail-icon.stream { color: #8944ab; background: rgb(175 82 222 / 10%); }
  .rail-icon.cache { color: #007aff; background: rgb(0 122 255 / 10%); }
  .rail-item.provider { grid-template-columns: 14px minmax(0, 1fr) auto; }
  .provider-dot { width: 7px; height: 7px; background: #007aff; }
  .rail-empty { color: #8e8e93; font-size: 11px; }
  .rail-select select,
  .search-field input,
  .compact-filters select,
  .settings-token input {
    border: 1px solid rgb(60 60 67 / 18%);
    background: rgb(255 255 255 / 78%);
    color: #1d1d1f;
  }
  .rail-select select { height: 34px; font-size: 12px; }
  .clear-filters { border-color: rgb(60 60 67 / 18%); color: #6e6e73; font-size: 11px; }
  .rail-footer { border-top-color: var(--line); color: #8e8e93; font-size: 10px; }

  .request-inbox { border-right: 1px solid var(--line); background: rgb(255 255 255 / 92%); }
  .inbox-header,
  .reading-header {
    border-bottom: 1px solid var(--line);
    background: rgb(255 255 255 / 72%);
    backdrop-filter: blur(22px);
  }
  .inbox-header { min-height: 62px; padding: 10px 14px; }
  .inbox-header h2,
  .reading-header h2,
  .diagnostics-header h2 { color: #1d1d1f; font-size: 16px; font-weight: 650; }
  .inbox-header p { color: #8e8e93; font-size: 11px; }
  .count-pill {
    min-width: 26px;
    min-height: 24px;
    border: 0;
    background: rgb(118 118 128 / 10%);
    color: #636366;
    font-size: 11px;
    font-weight: 600;
  }
  .inbox-tools { min-height: 52px; padding: 8px 10px; border-bottom-color: var(--line); }
  .search-field { position: relative; }
  .search-field::before {
    content: "";
    position: absolute;
    z-index: 1;
    top: 12px;
    left: 12px;
    width: 8px;
    height: 8px;
    border: 1.5px solid #8e8e93;
    border-radius: 50%;
    pointer-events: none;
  }
  .search-field::after {
    content: "";
    position: absolute;
    z-index: 1;
    top: 21px;
    left: 20px;
    width: 5px;
    height: 1.5px;
    border-radius: 2px;
    background: #8e8e93;
    transform: rotate(45deg);
    transform-origin: left center;
    pointer-events: none;
  }
  .search-field input { height: 36px; padding: 0 11px 0 31px; border: 0; border-radius: 10px; background: rgb(118 118 128 / 9%); font-size: 12px; }
  input::placeholder { color: #8e8e93; }
  .compact-filters { display: none; }
  .inbox-columns { display: none; }
  .inbox-list { border-top: 1px solid var(--line-soft); }
  .inbox-item {
    gap: 3px;
    min-height: 86px;
    padding: 9px 12px 9px 14px;
    border-bottom: 1px solid var(--line-soft);
    background: transparent;
    color: #3a3a3c;
  }
  .inbox-item:hover { background: rgb(118 118 128 / 6%); }
  .inbox-item.selected { background: #007aff; color: #fff; }
  .inbox-item.selected::before { background: transparent; }
  .inbox-item-top { grid-template-columns: 28px minmax(0, 1fr) auto; gap: 8px; }
  .provider-avatar {
    display: grid;
    place-items: center;
    width: 27px;
    height: 27px;
    border-radius: 50%;
    background: linear-gradient(145deg, #64d2ff, #0a84ff);
    color: #fff;
    font-size: 10px;
    font-weight: 700;
  }
  .inbox-identity { display: flex; align-items: baseline; gap: 6px; min-width: 0; }
  .inbox-identity strong { flex: 0 0 auto; color: #1d1d1f; font-size: 12px; font-weight: 650; }
  .inbox-model { color: #6e6e73; font-family: inherit; font-size: 12px; font-weight: 400; }
  .inbox-item-top time { color: #8e8e93; font-size: 10px; }
  .inbox-preview {
    display: block;
    overflow: hidden;
    color: #3a3a3c;
    font-size: 12px;
    line-height: 17px;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .inbox-meta-row { display: grid; grid-template-columns: minmax(94px, 1fr) auto; align-items: center; gap: 8px; min-width: 0; }
  .inbox-endpoint { display: flex; min-width: 0; gap: 5px; }
  .inbox-endpoint .mono { color: #8e8e93; font-size: 10px; }
  .inbox-item-bottom { gap: 6px; justify-content: flex-end; }
  .inbox-stat { color: #8e8e93; font-size: 10px; }
  .inbox-stat.cache { color: #007aff; }
  .inbox-item.selected .inbox-identity strong,
  .inbox-item.selected .inbox-model,
  .inbox-item.selected .inbox-preview,
  .inbox-item.selected .inbox-endpoint .mono,
  .inbox-item.selected .inbox-stat,
  .inbox-item.selected time { color: #fff; }
  .inbox-item.selected .provider-avatar { background: rgb(255 255 255 / 20%); box-shadow: inset 0 0 0 1px rgb(255 255 255 / 28%); }

  .provider-chip,
  .status-chip,
  .method-chip,
  .mode-chip { border: 0; box-shadow: none; }
  .provider-chip { padding: 2px 7px; border-radius: 6px; background: rgb(0 122 255 / 10%); color: #007aff; font-size: 10px; font-weight: 600; }
  .method-chip { padding: 1px 4px; border-radius: 4px; background: rgb(175 82 222 / 10%); color: #8944ab; font-size: 9px; font-weight: 600; }
  .status-chip { padding: 2px 7px; border-radius: 999px; font-size: 10px; font-weight: 600; }
  .status-chip.success { background: rgb(52 199 89 / 12%); color: #248a3d; }
  .status-chip.error { background: rgb(255 59 48 / 10%); color: #d70015; }
  .status-chip.warning { background: rgb(255 159 10 / 14%); color: #a05a00; }
  .mode-chip { padding: 2px 6px; border-radius: 999px; background: rgb(175 82 222 / 10%); color: #8944ab; font-size: 9px; font-weight: 600; }
  .inbox-item.selected .method-chip,
  .inbox-item.selected .status-chip,
  .inbox-item.selected .mode-chip { background: rgb(255 255 255 / 18%); color: #fff; }

  .reading-pane { container-type: inline-size; background: #fff; }
  .reading-header { min-height: 76px; padding: 13px 20px; background: rgb(255 255 255 / 82%); backdrop-filter: blur(22px); }
  .reading-eyebrow { margin-bottom: 5px; }
  .reading-title h2 { color: #1d1d1f; font-size: 21px; font-weight: 650; letter-spacing: -.02em; }
  .reading-route { color: #8e8e93; font-size: 11px; }
  .reading-id { display: none; }
  .reading-empty, .empty-state { color: #8e8e93; }
  .reading-empty strong, .empty-state strong { color: #3a3a3c; }
  .reading-empty-icon { border: 0; border-radius: 13px; background: rgb(0 122 255 / 9%); color: #007aff; }

  .facts-strip {
    display: grid;
    grid-template-columns: 70px 82px 122px minmax(120px, 1fr) 76px;
    flex: 0 0 auto;
    min-height: 54px;
    margin: 0 18px 8px;
    border: 0;
    border-radius: 12px;
    background: rgb(118 118 128 / 6%);
  }
  .fact { min-width: 0; padding: 8px 10px; }
  .fact span,
  .meta-label {
    display: block;
    color: #8e8e93;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1.15;
    text-transform: none;
  }
  .fact strong {
    display: block;
    margin-top: 4px;
    overflow: hidden;
    color: #3a3a3c;
    font-size: 12px;
    font-weight: 600;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .detail-tabs {
    align-self: flex-start;
    gap: 2px;
    min-height: 36px;
    margin: 0 18px 10px;
    padding: 3px;
    border: 0;
    border-radius: 10px;
    background: rgb(118 118 128 / 12%);
  }
  .detail-tab { min-height: 30px; padding: 0 11px; border: 0; border-radius: 7px; color: #6e6e73; font-size: 11px; font-weight: 600; }
  .detail-tab span { background: rgb(118 118 128 / 11%); color: #6e6e73; font-size: 9px; }
  .detail-tab:hover { color: #1d1d1f; }
  .detail-tab.selected { border: 0; background: #fff; color: #1d1d1f; box-shadow: 0 1px 3px rgb(0 0 0 / 12%); }

  .overview-grid {
    display: grid;
    grid-template-columns: minmax(0, 44%) minmax(0, 56%);
    flex: 1 1 auto;
    min-height: 0;
    height: 100%;
    padding: 0 18px 18px;
    background: #f5f5f7;
    gap: 14px;
  }
  .overview-card {
    display: grid;
    grid-template-rows: 50px minmax(0, 1fr);
    min-width: 0;
    min-height: 0;
    border: 1px solid rgb(60 60 67 / 12%);
    border-radius: 16px;
    background: rgb(255 255 255 / 92%);
    box-shadow: 0 1px 2px rgb(0 0 0 / 5%);
    overflow: hidden;
  }
  .response-overview { background: linear-gradient(180deg, rgb(255 255 255 / 96%), rgb(247 251 255 / 96%)); }
  .overview-card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    padding: 9px 14px;
    border-bottom: 1px solid var(--line-soft);
    color: #8e8e93;
    font-size: 10px;
  }
  .overview-card-header > div { display: grid; gap: 1px; }
  .overview-card-header strong { color: #3a3a3c; font-size: 12px; font-weight: 600; }
  .overview-kicker { color: #007aff; font-size: 10px; font-weight: 600; }
  .overview-card-body { min-height: 0; overflow: auto; padding: 14px; }
  .request-thread { display: grid; align-content: start; gap: 8px; }
  .overview-message { margin: 0; padding: 10px 0; border-bottom: 1px solid var(--line-soft); }
  .overview-message:last-child { border-bottom: 0; }
  .message-role,
  .turn-role { color: #6e6e73; font-size: 10px; font-weight: 600; letter-spacing: .01em; text-transform: capitalize; }
  .message-content { margin-top: 4px; color: #3a3a3c; font-size: 12px; line-height: 1.55; white-space: pre-wrap; overflow-wrap: anywhere; }
  .collapsed-message {
    padding: 0;
    border: 0;
    border-radius: 10px;
    background: rgb(118 118 128 / 7%);
    overflow: hidden;
  }
  .collapsed-message summary {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 8px;
    padding: 9px 10px;
    cursor: pointer;
    list-style: none;
  }
  .collapsed-message summary::-webkit-details-marker { display: none; }
  .message-preview { overflow: hidden; color: #6e6e73; font-size: 11px; text-overflow: ellipsis; white-space: nowrap; }
  .message-size, .turn-size { color: #8e8e93; font-size: 10px; white-space: nowrap; }
  .collapsed-message[open] .message-content { max-height: 320px; margin: 0; padding: 10px; border-top: 1px solid var(--line-soft); overflow: auto; }
  .response-body { display: grid; align-content: start; gap: 10px; padding: 22px; }
  .response-copy { color: #1d1d1f; font-size: 16px; line-height: 1.62; letter-spacing: -.005em; white-space: pre-wrap; overflow-wrap: anywhere; }
  .response-json {
    width: 100%;
    min-width: 0;
    margin: 0;
    overflow: auto;
    color: #2c2c2e;
    font-family: "SFMono-Regular", "SF Mono", ui-monospace, Menlo, Monaco, Consolas, monospace;
    font-size: 12px;
    font-variant-ligatures: none;
    letter-spacing: 0;
    line-height: 1.65;
    tab-size: 2;
    white-space: pre-wrap;
    overflow-wrap: break-word;
  }

  .panel-heading { min-height: 42px; padding: 8px 14px; border-bottom-color: var(--line-soft); }
  .panel-heading h3 { color: #3a3a3c; font-size: 13px; font-weight: 600; }
  .panel-heading span { color: #8e8e93; font-size: 10px; }
  .retention-warning { border-bottom-color: rgb(255 159 10 / 24%); background: rgb(255 159 10 / 9%); color: #a05a00; }
  .conversation-scroll { padding: 18px; background: #f5f5f7; }
  .conversation { gap: 12px; max-width: 820px; }
  .turn {
    padding: 11px 13px;
    border: 1px solid rgb(60 60 67 / 12%);
    border-radius: 13px;
    background: rgb(255 255 255 / 92%);
    color: #3a3a3c;
    box-shadow: 0 1px 2px rgb(0 0 0 / 4%);
  }
  .turn::before { display: none; }
  .turn-content { margin-top: 5px; color: #3a3a3c; font-size: 13px; line-height: 1.55; }
  .turn[data-response-card] { border-color: rgb(0 122 255 / 20%); background: linear-gradient(180deg, #fff, #f4f9ff); }
  .turn[data-response-card] .turn-content { max-height: 280px; overflow: auto; font-size: 14px; }
  .response-card-header { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
  .response-card-header > div { display: grid; gap: 1px; }
  .response-card-header > div > span { color: #8e8e93; font-size: 10px; }
  .thread-heading { display: flex; align-items: baseline; justify-content: space-between; gap: 10px; padding: 8px 2px 2px; }
  .thread-heading strong { color: #3a3a3c; font-size: 13px; font-weight: 600; }
  .thread-heading span { color: #8e8e93; font-size: 10px; }
  .turn-disclosure { padding: 0; }
  .turn-disclosure summary {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 9px;
    padding: 10px 12px;
    cursor: pointer;
    list-style: none;
  }
  .turn-disclosure summary::-webkit-details-marker { display: none; }
  .turn-preview { overflow: hidden; color: #6e6e73; font-size: 11px; text-overflow: ellipsis; white-space: nowrap; }
  .turn-disclosure[open] .turn-content { max-height: 360px; margin: 0; padding: 12px; border-top: 1px solid var(--line-soft); overflow: auto; }

  .wire-grid { background: var(--line); }
  .payload-card { background: #f7f7f9; }
  .payload-head { border-bottom-color: var(--line); color: #6e6e73; font-size: 10px; font-weight: 600; letter-spacing: 0; }
  .payload-content { color: #2c2c2e; font-size: 11px; line-height: 1.55; }
  .chunk { border-bottom-color: var(--line-soft); }
  .chunk-index { color: #8e8e93; font-size: 10px; }
  .chunk-content { color: #3a3a3c; font-size: 12px; }
  .runtime-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 9px; padding: 14px; }
  .meta-card { border: 0; border-radius: 12px; background: rgb(118 118 128 / 7%); }
  .meta-value { color: #3a3a3c; font-size: 11px; }
  .runtime-note { border: 0; border-radius: 12px; background: rgb(118 118 128 / 7%); }
  .runtime-note p { color: #3a3a3c; }

  .diagnostics-sheet {
    top: 38px;
    right: 0;
    width: min(520px, calc(100vw - 24px));
    max-height: calc(100vh - 66px);
    border: 1px solid rgb(60 60 67 / 16%);
    border-radius: 16px;
    background: rgb(250 250 252 / 97%);
    color: #1d1d1f;
    box-shadow: 0 18px 54px rgb(0 0 0 / 18%);
    backdrop-filter: saturate(180%) blur(30px);
  }
  .diagnostics-header { border-bottom-color: var(--line); }
  .diagnostics-header .eyebrow { color: #8e8e93; }
  .retention-note { color: #8e8e93; }
  .runtime-settings { display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: end; gap: 10px; padding: 11px 12px; border-bottom: 1px solid var(--line); background: rgb(242 242 247 / 92%); }
  .settings-token { display: grid; gap: 4px; color: #6e6e73; font-size: 10px; font-weight: 600; }
  .settings-token input { width: 100%; height: 34px; padding: 0 9px; border-radius: 5px; }
  .diagnostics-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); background: transparent; }
  .diagnostic-card { border-color: rgb(60 60 67 / 12%); border-radius: 12px; background: rgb(255 255 255 / 96%); }
  .diagnostic-card h3 { border-bottom-color: var(--line-soft); color: #3a3a3c; font-size: 11px; }
  .diagnostic-card:last-child { grid-column: span 2; }
  .diagnostic-row { border-top-color: var(--line-soft); }
  .diagnostic-primary { color: #3a3a3c; font-size: 12px; }
  .diagnostic-secondary { color: #8e8e93; font-size: 11px; }
  .diagnostic-empty { color: #8e8e93; }
  .runtime-dialog-trigger { gap: 7px; }
  .runtime-fluent-dialog {
    --runtime-dialog-width: min(980px, calc(100vw - 32px));
    --runtime-dialog-height: min(760px, calc(100dvh - 48px));
    position: absolute !important;
    z-index: 999 !important;
    inset: 0 !important;
    width: 100vw !important;
    height: 100dvh !important;
  }
  .runtime-fluent-dialog::part(positioning-region) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }
  .runtime-fluent-dialog::part(overlay) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    background: rgb(10 10 12 / 20%);
    backdrop-filter: blur(8px) saturate(140%);
  }
  .runtime-fluent-dialog::part(control) {
    --dialog-padding: 0px;
    box-sizing: border-box;
    width: var(--runtime-dialog-width) !important;
    height: var(--runtime-dialog-height) !important;
    max-width: calc(100vw - 32px);
    max-height: calc(100dvh - 48px);
    padding: 0;
    border: 1px solid rgb(255 255 255 / 72%);
    border-radius: 18px;
    background: rgb(250 250 252 / 96%);
    box-shadow:
      inset 0 1px 0 rgb(255 255 255 / 92%),
      0 22px 60px -12px rgb(10 10 12 / 24%);
    backdrop-filter: blur(36px) saturate(180%);
    overflow: hidden;
  }
  .runtime-fluent-dialog .diagnostics-sheet {
    position: static;
    width: 100%;
    height: 100%;
    max-height: none;
    overflow: auto;
    border: 0;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
  }
  .runtime-fluent-dialog .diagnostics-header {
    position: sticky;
    z-index: 3;
    top: 0;
    background: rgb(250 250 252 / 94%);
    backdrop-filter: blur(24px) saturate(160%);
  }
  .dialog-header-actions { display: flex; align-items: center; gap: 10px; }
  .dialog-close {
    display: grid;
    place-items: center;
    width: 34px;
    height: 34px;
    padding: 0;
    border: 0;
    border-radius: 50%;
    background: rgb(118 118 128 / 10%);
    color: #6e6e73;
    cursor: pointer;
    font-size: 18px;
    line-height: 1;
  }
  .dialog-close:hover { background: rgb(118 118 128 / 18%); color: #1d1d1f; }

  /* Brand layer: Prostir technical polish × Managed Code editorial warmth. */
  :root {
    --canvas: #f9f7f5;
    --pane-selected: rgb(164 242 88 / 14%);
    --line: rgb(22 22 22 / 13%);
    --line-soft: rgb(22 22 22 / 8%);
    --text: #161616;
    --text-soft: #363432;
    --muted: rgb(18 18 18 / 60%);
    --accent: #a4f258;
    --accent-strong: #83cc3c;
    --green: #3f8d55;
    --red: #c94f3d;
    --amber: #946a17;
    --brand-coral: #ff7e5d;
    --brand-magenta: #db3276;
  }
  html, body { background: #f9f7f5; }
  body { color: #161616; }
  button:focus-visible, input:focus-visible, select:focus-visible, summary:focus-visible {
    box-shadow: 0 0 0 2px #fff, 0 0 0 5px rgb(164 242 88 / 72%);
  }
  .shell {
    background:
      radial-gradient(700px 440px at 4% -12%, rgb(164 242 88 / 10%), transparent 62%),
      radial-gradient(660px 420px at 97% -18%, rgb(255 126 93 / 8%), transparent 58%),
      #f9f7f5;
  }
  .topbar {
    position: relative;
    border-bottom-color: rgb(22 22 22 / 10%);
    background:
      linear-gradient(135deg, rgb(255 255 255 / 80%), rgb(249 247 245 / 78%)),
      rgb(249 247 245 / 84%);
  }
  .topbar::after {
    content: "";
    position: absolute;
    right: 0;
    bottom: -1px;
    left: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 2%, rgb(164 242 88 / 82%) 28%, rgb(219 50 118 / 58%) 66%, rgb(237 115 46 / 62%) 84%, transparent 98%);
    pointer-events: none;
  }
  .brand-mark {
    position: relative;
    background: linear-gradient(145deg, #2a2a2a, #121212 72%);
    color: #fff;
    box-shadow: 0 4px 12px rgb(22 22 22 / 16%), inset 0 1px rgb(255 255 255 / 18%);
  }
  .brand-mark::after {
    content: "";
    position: absolute;
    top: 5px;
    right: 5px;
    width: 6px;
    height: 6px;
    border-radius: 2px;
    background: #a4f258;
    box-shadow: 0 0 0 1px rgb(164 242 88 / 22%);
  }
  .brand h1 { color: #161616; font-weight: 700; letter-spacing: -.025em; }
  .toolbar-button.accent {
    background: #a4f258;
    color: #121212;
    box-shadow: 0 5px 16px rgb(105 156 53 / 18%), inset 0 1px rgb(255 255 255 / 42%);
  }
  .toolbar-button.accent:hover { background: #b2fa68; color: #121212; }
  .runtime-dialog-trigger { background: #161616; color: #fff; }
  .runtime-dialog-trigger:hover { background: #2a2928; color: #fff; }
  .auto-toggle input { accent-color: #83cc3c; }
  .connection.ok .status-dot { background: #5bc97a; }
  .connection.warn .status-dot { background: #e8b445; }
  .connection.err .status-dot { background: #e87c66; }

  .nav-rail { background: rgb(242 237 232 / 82%); }
  .rail-item.selected {
    background: rgb(164 242 88 / 20%);
    color: #263a14;
    box-shadow: inset 3px 0 #a4f258;
  }
  .rail-clear { color: #9f382b; }
  .rail-icon.cache { color: #346d82; background: rgb(95 182 220 / 14%); }
  .provider-dot { background: #a4f258; box-shadow: 0 0 0 1px rgb(22 22 22 / 18%); }
  .rail-select select,
  .search-field input,
  .compact-filters select,
  .settings-token input { border-color: rgb(22 22 22 / 13%); }

  .request-inbox,
  .reading-pane { background: rgb(255 255 255 / 94%); }
  .inbox-header,
  .reading-header {
    border-bottom-color: rgb(22 22 22 / 10%);
    background: linear-gradient(135deg, rgb(255 255 255 / 88%), rgb(255 248 244 / 58%));
  }
  .inbox-header h2,
  .reading-header h2,
  .diagnostics-header h2 { color: #161616; font-weight: 700; letter-spacing: -.025em; }
  .provider-avatar {
    background: linear-gradient(145deg, #30302f, #111 75%);
    color: #a4f258;
    box-shadow: inset 0 1px rgb(255 255 255 / 14%);
  }
  .inbox-item.selected {
    background: linear-gradient(90deg, rgb(164 242 88 / 19%), rgb(255 255 255 / 98%) 38%);
    color: #161616;
    box-shadow: inset 3px 0 #a4f258;
  }
  .inbox-item.selected::before { background: #a4f258; }
  .inbox-item.selected .inbox-identity strong,
  .inbox-item.selected .inbox-model,
  .inbox-item.selected .inbox-preview,
  .inbox-item.selected .inbox-endpoint .mono,
  .inbox-item.selected .inbox-stat,
  .inbox-item.selected time { color: #242321; }
  .inbox-item.selected .inbox-endpoint .mono,
  .inbox-item.selected .inbox-stat,
  .inbox-item.selected time { color: rgb(18 18 18 / 60%); }
  .inbox-item.selected .inbox-stat.cache { color: #4d781e; }
  .inbox-item.selected .provider-avatar {
    background: #161616;
    color: #a4f258;
    box-shadow: inset 0 0 0 1px rgb(255 255 255 / 12%);
  }
  .inbox-item.selected .method-chip { background: rgb(137 68 171 / 10%); color: #71398c; }
  .inbox-item.selected .status-chip.success { background: rgb(91 201 122 / 16%); color: #337c49; }
  .inbox-item.selected .status-chip.error { background: rgb(232 124 102 / 15%); color: #b83f31; }
  .inbox-item.selected .mode-chip { background: rgb(219 50 118 / 10%); color: #a3255b; }
  .provider-chip { background: rgb(164 242 88 / 22%); color: #365b16; }
  .inbox-stat.cache { color: #4d781e; }
  .status-chip.success { background: rgb(91 201 122 / 15%); color: #337c49; }
  .status-chip.error { background: rgb(232 124 102 / 14%); color: #b83f31; }
  .status-chip.warning { background: rgb(232 180 69 / 16%); color: #836018; }

  .facts-strip { background: rgb(242 237 232 / 72%); }
  .detail-tab { position: relative; }
  .detail-tab.selected::after {
    content: "";
    position: absolute;
    right: 8px;
    bottom: 2px;
    left: 8px;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, #db3276, #ed732e);
  }
  .overview-grid,
  .conversation-scroll { background: #f9f7f5; }
  .overview-card { border-color: rgb(22 22 22 / 10%); }
  .response-overview {
    position: relative;
    background: linear-gradient(135deg, #fff 44%, rgb(255 126 93 / 5%), rgb(219 50 118 / 3.5%));
  }
  .response-overview::before {
    content: "";
    position: absolute;
    z-index: 2;
    top: 0;
    right: 0;
    left: 0;
    height: 2px;
    background: linear-gradient(90deg, #a4f258, #ff7e5d 60%, #db3276);
  }
  .overview-kicker { color: #a63b2a; }
  .turn[data-response-card] {
    border-color: rgb(164 242 88 / 46%);
    background: linear-gradient(135deg, #fff, rgb(164 242 88 / 7%), rgb(255 126 93 / 4%));
  }
  .payload-card { background: #f7f3ef; }
  .meta-card,
  .runtime-note { background: rgb(242 237 232 / 72%); }

  .runtime-fluent-dialog::part(control) {
    border-color: rgb(255 255 255 / 88%);
    background: rgb(249 247 245 / 96%);
    box-shadow:
      inset 0 1px 0 rgb(255 255 255 / 94%),
      0 24px 70px -16px rgb(22 22 22 / 30%);
  }
  .runtime-fluent-dialog .diagnostics-header {
    position: sticky;
    background: rgb(249 247 245 / 94%);
  }
  .runtime-fluent-dialog .diagnostics-header::after {
    content: "";
    position: absolute;
    right: 0;
    bottom: -1px;
    left: 0;
    height: 2px;
    background: linear-gradient(90deg, #a4f258, #db3276 64%, #ed732e);
    opacity: .72;
  }
  .runtime-settings { background: rgb(242 237 232 / 78%); }
  .diagnostic-card { border-color: rgb(22 22 22 / 10%); background: rgb(255 255 255 / 88%); }
  .dialog-close { background: #161616; color: #fff; }
  .dialog-close:hover { background: #a4f258; color: #161616; }

  @@media (max-width: 1439px) {
    .mail-workspace { grid-template-columns: 184px 360px minmax(0, 1fr); }
    .nav-rail { display: flex; }
    .compact-filters { display: none; }
    .wire-grid { grid-template-columns: 1fr; grid-template-rows: repeat(2, minmax(300px, 1fr)); overflow: auto; }
  }

  @@media (max-width: 1099px) {
    .mail-workspace { grid-template-columns: 350px minmax(0, 1fr); }
    .nav-rail { display: none; }
    .compact-filters { display: grid; grid-template-columns: 1fr 1fr; }
    .compact-filters select:last-of-type { grid-column: 1 / -1; }
    .inbox-tools { gap: 7px; }
    .runtime-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }

  @@media (max-width: 900px) {
    .mail-workspace { grid-template-columns: 330px minmax(0, 1fr); }
    .facts-strip { grid-template-columns: 66px 76px 116px minmax(110px, 1fr); }
    .fact:last-child { display: none; }
  }

  @@media (max-width: 767px) {
    .shell { grid-template-rows: 48px minmax(0, 1fr); }
    .topbar { padding: 0 8px; }
    .brand { min-width: 0; }
    .brand-full { display: none; }
    .brand-short { display: inline; }
    .brand h1 { font-size: 14px; }
    .command-bar { gap: 4px; }
    .connection { min-width: 28px; padding: 0 7px; }
    .auto-toggle { min-width: 32px; padding: 0 7px; }
    .command-bar > .toolbar-button.accent { width: 34px; padding: 0; }
    .command-bar > .toolbar-button.accent span:last-child { display: none; }
    .runtime-dialog-trigger { width: 34px; padding: 0; font-size: 12px; }
    .runtime-dialog-trigger span:first-child { display: none; }
    .runtime-fluent-dialog {
      --runtime-dialog-width: calc(100vw - 12px);
      --runtime-dialog-height: calc(100dvh - 12px);
    }
    .runtime-fluent-dialog::part(control) {
      max-width: calc(100vw - 12px);
      max-height: calc(100dvh - 12px);
      border-radius: 18px;
    }
    .mail-workspace { grid-template-columns: minmax(0, 1fr); }
    .mobile-list .reading-pane { display: none; }
    .mobile-reading .request-inbox { display: none; }
    .inbox-header { min-height: 58px; }
    .inbox-item { min-height: 86px; }
    .reading-header { min-height: 68px; padding: 10px 12px; }
    .facts-strip { grid-template-columns: repeat(4, minmax(92px, 1fr)); overflow-x: auto; }
    .fact:last-child { display: none; }
    .overview-grid { grid-template-columns: 1fr; grid-template-rows: minmax(220px, 42%) minmax(0, 1fr); overflow: hidden; }
    .response-overview { order: -1; }
    .overview-card-header { min-height: 46px; }
    .response-body { padding: 16px; }
    .response-copy { font-size: 14px; }
    .conversation-scroll { padding: 12px; }
    .thread-heading { display: grid; }
    .wire-grid { grid-template-columns: 1fr; grid-template-rows: repeat(2, minmax(320px, 1fr)); overflow: auto; scroll-snap-type: none; }
    .payload-card { scroll-snap-align: none; }
    .diagnostics-sheet { top: 54px; right: 8px; left: 8px; width: auto; max-height: calc(100vh - 62px); }
    .diagnostics-grid { grid-template-columns: 1fr; }
    .diagnostic-card:last-child { grid-column: auto; }
    .runtime-settings { grid-template-columns: 1fr; }
  }

  @@media (max-width: 480px) {
    .compact-filters { grid-template-columns: 1fr 1fr; }
    .compact-filters select:first-child { grid-column: 1 / -1; }
    .inbox-meta-row { grid-template-columns: minmax(80px, 1fr) auto; }
    .inbox-item-bottom .inbox-stat:nth-of-type(n + 3) { display: none; }
    .reading-title h2 { font-size: 17px; }
    .detail-tabs { padding-left: 7px; padding-right: 7px; }
    .detail-tab { padding: 0 8px; }
  }
</style>
</head>
<body data-renderer="blazor-ssr">
<LlmTckAdminPanel @rendermode="@(new InteractiveServerRenderMode())" />
<script src="_framework/blazor.web.js"></script>
</body>
</html>
