# ManagedCode.LlmTck.Groq

This package owns Groq compatibility constants, provider profiles, and future Groq wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
