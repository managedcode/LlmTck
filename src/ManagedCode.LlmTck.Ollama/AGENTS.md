# ManagedCode.LlmTck.Ollama

This package owns Ollama compatibility constants, provider profiles, and future Ollama wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
