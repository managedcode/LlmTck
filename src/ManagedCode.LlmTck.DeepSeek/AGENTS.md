# ManagedCode.LlmTck.DeepSeek

This package owns DeepSeek compatibility constants, provider profiles, and future DeepSeek wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
