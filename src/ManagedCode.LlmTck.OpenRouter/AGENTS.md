# ManagedCode.LlmTck.OpenRouter

This package owns OpenRouter compatibility constants, provider profiles, and future OpenRouter wire contracts.

Keep deterministic runtime behavior in `ManagedCode.LlmTck`; keep HTTP hosting in `ManagedCode.LlmTck.Hosting`.
